Все три пункта закрыты корректно, разобрал по каждому.

**Catch-all exception** — точечное изменение, `except Exception` полностью заменяет узкий tuple, ничего лишнего не тронуто.

**`position_plan` в моке** — чистое расширение (backward-compatible, старые тесты без `position_plan` продолжают использовать `self.positions` как раньше), позволяет корректно моделировать разные ответы на первый и второй `get_position()`.

**Тест на полную TP-гонку** (`test_reversal_tp_full_race_continues_as_normal_target_entry`) — это даже сильнее, чем я просил: проверяет не изолированный `_run_reversal_close`, а весь путь через `_submit_opposite` целиком. `client.close_calls == []` доказывает, что explicit close вообще не понадобился; `entry_calls[0]["direction"] == "SHORT"` и `origin="signal"/reversal_id=NULL` в БД — прямое подтверждение того самого сценария, который мы разбирали по буквальному коду вызывающей стороны в прошлом сообщении. Снимает вопрос №1 полностью, с end-to-end доказательством, а не только по чтению кода.

**Тест на смену направления** — `position_plan` из двух записей (Buy→Sell) корректно подаёт разные состояния на `before` и `after_tp_cancel`; `recovery_reason` совпадает буква в букву с кодом.

**Тест на неожиданную ошибку** (`RuntimeError`, не `BybitDemoError`/`ValueError`) — именно то, что нужно для проверки расширенного catch-all; `close_position_read_after_tp_cancel_failed:unexpected_position_payload` подтверждает, что f-string и recovery-путь работают на произвольном исключении, а не только на ожидаемых типах.

Числа сходятся: 107→110 (3 новых теста), targeted 9→12. Никаких пробелов не осталось — все ветки `_run_reversal_close()`, введённые этим этапом (best-effort cancel, ambiguous vs terminal ошибки, stale-quantity fix, полная TP-гонка, смена направления, произвольная ошибка post-cancel read, изоляция по source ledgers) теперь имеют явный тест.

**Этап 4 принят.**

Важное замечание на будущее, не про этот diff, а про весь проект целиком: этапы 1–4 покрыли модель данных, ATR-калькулятор, read-only reconciliation и reversal-cleanup — но письмо (`letter_partial_tp_system.md`) также требовало:

- **п.4** — механику размещения самих N частичных reduce-only TP-ордеров на Bybit при открытии позиции (замена единого TP-поля в `create_market_order`) — этого не было ни в одном из четырёх этапов;
- **п.5 / Done-чеклист** — реальный перенос SL в безубыток после подтверждённого TP1 (поля `be_state`/`be_price`/`be_set_ts` есть в схеме с этапа 1, но код, который их выставляет и реально сдвигает SL, я не видел ни в одном diff).

Без этих двух частей фича не завершена по исходному скоупу — сейчас построена модель и её обслуживание (cancel/reconcile), но не сам механизм выставления и защиты частичных TP. Стоит явно решить, идём ли дальше отдельными этапами 5/6 на это, прежде чем считать тикет закрытым.