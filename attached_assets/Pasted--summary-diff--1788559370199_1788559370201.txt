Принимаю отчёт как *summary*, но не как diff — а следующий чекпоинт по установленному для всех тикетов процессу требует именно "diff + тесты на review", прежде чем переходить к отдельному "да" на деплой. Прошу прислать сам diff (или как минимум изменённые функции целиком), а не только список тезисов — по той же причине, по которой мы весь этот процесс не принимали "работает, поверьте" ни на одном из предыдущих этапов.

Конкретно нужно увидеть в коде, а не в описании:

1. **Значение флага по умолчанию** — буквальную строку объявления `BYBIT_DEMO_BREAKEVEN_ENABLED`, чтобы подтвердить `False`/off, а не поверить на слово.
2. **State machine** — реализацию переходов `not_armed → pending → armed / recovery_required`, включая место, где `be_pending_since_ts` и `be_readback_attempts` записываются/инкрементируются, и условие `timeout=180s OR attempts=3 → recovery_required`.
3. **Финальный re-check reversal перед `set_trading_stop()`** — именно то место в коде, где это происходит непосредственно перед вызовом API, а не только на этапе eligibility.
4. **Единую protection-level comparison функцию** — что она действительно одна и используется во всех четырёх ветках (успех, потерянный response, duplicate, уже существующий более защищённый native SL), а не продублирована с расхождениями.
5. **Fail-closed при смене live direction** во время `pending` — сам код перехода.
6. Сам diff по `.agents/memory/*` — что в MEMORY.md/bybit-tp-recovery-states.md зафиксировано корректно (новые recovery states, флаг, семантика), без искажений контракта.

По тестам — не только цифры прошедших (38/143/295), а список **какие конкретно кейсы покрыты** по чек-листу "Done looks like": TOCTOU-гонка BE↔reversal, downgrade-prevention на duplicate/lost-response, эскалация pending по timeout и по числу readback, fail-closed на смене direction, no-downgrade при уже установленном более защищённом native SL. Если что-то из этого списка не покрыто тестами — надо явно сказать, что осталось не протестированным, а не оставлять по умолчанию как "предположительно покрыто раз suite прошёл".

Как только diff придёт — отревьюю его против контракта из предыдущих раундов, включая пункт про симметрию гонки BE↔reversal, который я просил подтвердить отдельно. #200 (route-level тесты для `retry_breakeven`) — принимается как отдельный follow-up, не блокирует текущий review.