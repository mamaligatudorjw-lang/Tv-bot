Стоп — это не diff, это shell-команда, которая должна была бы его сгенерировать:

```bash
git diff --no-ext-diff -- telegram-webhook-bot/bybit_demo.py telegram-webhook-bot/app.py telegram-webhook-bot/tests/test_bybit_demo.py
```

Я не видел вывод этой команды. Дан только один вручную выбранный фрагмент из `app.py` ("ключевой фактический diff") — и полностью отсутствует:

- diff `bybit_demo.py` целиком: новый reduce-only Limit TP client method, orderLinkId-генерация, сама orchestration-логика `ensure_pending_tp_orders()`, классификатор create-ошибок (deterministic/duplicate/ambiguous — тот самый, что мы верифицировали построчно на этапе плана), deadline/`RECOVERY_REQUIRED` wiring, pending-flow state machine;
- diff 27 новых/изменённых тестов;
- остальная часть diff `app.py` за рамками показанного фрагмента.

На всём протяжении этого расследования правило было одно: без буквального diff — нет review, нет принятия. Именно построчное чтение (а не пересказ) нашло реальные баги на этапах 2 ("R-от-SL" вместо ATR), 3 (`PartiallyFilledCanceled`) и 4 (stale quantity). Этап 5 — самый чувствительный из всех (первое реальное размещение ордеров, пусть и за выключенным флагом), и именно здесь пересказ неприемлем более, чем где-либо. Пришли, пожалуйста, полный текстовый вывод этой git diff команды — то есть сам diff, а не рецепт его получения.

Дополнительно нужно объяснение по одному пункту, который не был частью согласованного плана этапа 5:

**"Telegram Webhook Bot workflow перезапущен и успешно поднялся."** Рестарт live-сервиса — это реальное операционное событие, даже если feature flag выключен. В плане этапа 5 обсуждалось, что canary/production/deploy остаются неизменными — рестарт воркфлоу это как минимум затрагивает. Уточни:
- было ли это осознанно запланированное действие (нужно ли было перезапускать сервис, чтобы подхватить новый импорт `ensure_pending_tp_orders` и новый код), или последствие деплоя, которое не обсуждалось заранее;
- сбрасывало ли это in-memory state (`state["atr_4h"]`, `state["atr_4h_close_ts"]`, и что-либо ещё, живущее только в памяти процесса) — и если да, затронуло ли это уже открытые позиции/активные reversals, которые могли зависеть от этого in-memory state, а не только от SQLite;
- было ли на это отдельное разрешение, или это подразумевалось как часть "внести diff" без явного обсуждения рестарта сервиса.

Также, когда придёт полный diff — арифметика тестов: было "110 passed" на конец этапа 4, сейчас "126 passed" (+16), но "targeted: 27 passed" не сходится ни с этой разницей, ни с прежним паттерном объяснения (targeted = новые + часть существующих). Объясни состав, как в предыдущих раундах.

Жду полный буквальный diff и ответ по рестарту перед началом review.