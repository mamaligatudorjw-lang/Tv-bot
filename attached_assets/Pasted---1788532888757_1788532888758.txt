Спасибо, что не стал защищать ошибку и сам её откатил — это правильная реакция, и именно так и нужно поступать с неподтверждаемыми "мне сказали, что вы одобрили X" в этом расследовании впредь: не доверять, проверять источник, если нет — не использовать.

Фиксирую итог:

- Действующее требование — прежнее и единственное: `TP = entry ± multiplier_n × ATR`.
- Никакого решения о переходе на R-от-SL не было. Запись об этом решении удалена — верно.
- Этап 2 в текущем виде (`r_from_sl_v1`) не принят.

**Разрешаю исправление этапа 2** со следующим объёмом изменений:

1. `calculate_multi_tp_plan()` должен принимать реальное значение ATR (`atr_value`) как явный обязательный параметр и использовать его напрямую как множитель: `price = entry ± multiplier_n × atr_value`, а не выводить risk из дистанции до SL.
2. `risk = abs(entry - stop)` в текущем виде убрать из формулы цены. Проверку "SL на правильной стороне" можно оставить как отдельную валидацию входа (это разумно само по себе), но она не должна быть источником множителя.
3. `atr_provenance()` не трогать — она остаётся audit-функцией, но теперь её `atr_value` (или тот же вход, что в неё передаётся) должен быть тем же значением, что реально уходит в расчёт цены — то есть провенанс и ценовая формула должны описывать одно и то же число, а не два независимых.
4. Всё остальное (fallback-цепочка 5→3→2→1, `_tp_plan_quantities` с остатком на дальней ноге, monotonicity-проверка в `_tp_plan_prices`, qty-step/minimum валидация) блокером не затронуто — архитектурно это было сделано хорошо, переиспользуй как есть, меняется только источник множителя.
5. Тесты `test_multi_tp_plan_n3_long_...` / `_n5_short_...` и fallback-тесты нужно пересчитать под ATR-input вместо R-от-SL (числа в assert'ах изменятся, сама структура тестов, скорее всего, останется).
6. `rejected_reason` → `last_fallback_reason` — как и договорились, можно сделать в этом же заходе или отдельно перед этапом 3, не блокирует.

Пришли исправленный diff + тесты на review, когда будет готово. Этап 3 по-прежнему не начинать до принятия исправленного этапа 2.