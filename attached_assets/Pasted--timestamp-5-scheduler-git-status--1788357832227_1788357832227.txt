Пункт с timestamp закрыт полностью — понятный механизм (5-минутный scheduler), чистый git status, чистое дерево. Принято.

По пункту 1 — ответ на самом деле открывает более важный вопрос, чем закрывает.

**"Также отдельно не подтверждалось, что Telegram production webhook направлен именно на workspace workflow"** — это ключевая фраза. Если это не подтверждено, то у нас нет доказательства и обратного: что workspace workflow **не** обслуживает реальный трафик. Различие "workspace workflow" vs "Published deployment" — это внутренняя категоризация Replit, но для нас имеет значение только одно: какой процесс физически получает вебхуки от Telegram и исполняет торговую логику (whitelist, exposure_cap, submit к Bybit) прямо сейчас. Если это фактически workspace workflow — то каждый его перезапуск, независимо от того, называется это "deployment" или нет, перезапускает живой боевой процесс.

Прошу проверить это напрямую, не через предположение о том, как "должно быть":

1. `getWebhookInfo` через Telegram Bot API — куда именно (URL) сейчас направлен production webhook.
2. Сопоставить этот URL с тем, что реально обслуживает workspace workflow (порт 5000/gunicorn) — совпадает или нет.

Пока это не проверено фактически, формулировка "Deployment не выполнялся" ничего не гарантирует по существу — только то, что не трогали объект деплоя в Replit, а не то, что живой трафик не был затронут.

По diff — два momента:

3. Показан diff только последнего коммита (`ab2ef20`). Сам основной код #182 (`_gateio_get()`, bounded semaphore acquire, wall-clock deadline, telemetry-события) — в предыдущих коммитах диапазона `8dbd399^..HEAD`, и его содержимое я не видел, только файл-лист и агрегированную статистику (908/-138 по 6 файлам). Пришли непосредственно diff по `telegram-webhook-bot/app.py` — это ядро изменения с наибольшим blast radius (затрагивает все Gate.io-вызовы, включая live-путь #186).
4. `attached_assets/Pasted---1788353526262_1788353526263.txt` в списке изменённых файлов — что это и почему входит в диапазон коммитов #182? Подтверди, что это не относится к торговой логике.

До ответа на эти 4 пункта — "да" на deployment не даю.