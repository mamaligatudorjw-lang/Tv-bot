Diff по `app.py` технически сильный и точно соответствует всем согласованным деталям: константы совпадают (`RANGE_BREAKOUT_MAX_INFLIGHT=8`, `RANGE_BREAKOUT_REQUEST_TIMEOUT=8.0`, `CONFIRM_GRACE=10.0`), exception-иерархия `_GateioTimeout`→`_GateioSemaphoreTimeout`/`_GateioRequestDeadline`/`_GateioRequestCancelled` как subclass от `requests.Timeout` — как договаривались, legacy callers сохраняют `timeout=10` по умолчанию, `_gateio_get()` через `try/finally` гарантированно освобождает permit и закрывает response независимо от исхода, а bounded ThreadPoolExecutor в range теперь безопасен даже для orphaned futures именно потому, что гарантия release перенесена в transport-слой, а не зависит от того, дождался ли executor этого future. Это закрывает системный риск, который я поднимал раньше про Bollinger/EMA. Код принимается.

Но пункт 1 вашего же ответа обнаружил вещь важнее самого diff, и её нужно закрыть прежде, чем говорить о deployment вообще.

## Главное: похоже, сейчас может существовать два независимых живых процесса, оба гоняющих торговую логику

Вы пишете: *"опубликованный процесс отдельный; в его deployment logs видны реальные scheduler cycles и `bybit_demo_poll`"* — и одновременно workspace workflow *"выполняет scheduler и исходящие операции, включая циклы сигналов и Bybit Demo polling"*.

Если оба утверждения верны буквально — это означает, что **два разных процесса** (workspace workflow и published deployment) **независимо** выполняют scan-циклы, генерируют сигналы и обращаются к Bybit Demo прямо сейчас, параллельно. Это меняет всё, включая доверие к предыдущим решениям:

- exposure_cap (#186/после) — каждый процесс видит свою локальную картину "текущей экспозиции" и независимо решает, укладывается ли новый ордер в лимит. Два процесса, каждый уверенный, что лимит не превышен, суммарно могут превысить его вдвое.
- duplicate guard — если это два процесса (а не два потока одного процесса), никакой in-process лок их не защитит от дублирующих ордеров, если только они не используют общую БД с транзакционным лочингом на уровне записи.
- Вся диагностика #182 (единственный worker, зависший на 10+ часов) — если было два независимых scheduler-процесса, не ясно, какой из них производил измеренные события, и не смешаны ли в логах события от разных процессов как будто это один.

**Прошу прямо и с доказательствами, не выводом:**

1. Сейчас одновременно запущены оба процесса (workspace workflow **и** published deployment)? Проверить явно — например, отдельные PID/порт для каждого, оба ли сейчас `running`.
2. Если оба активны — используют ли они одну и ту же базу данных (одна connection string/один Postgres instance), или у каждого своя? Это определяет, реален ли риск двойного счёта exposure_cap и дублей позиций, или это просто два параллельных report-генератора без реальной торговой логики в одном из них.
3. Если published deployment тоже реально сабмитит сигналы на Bybit — с каким кодом он сейчас запущен: с версией до #182-фикса или после? (Если #182 ещё не задеплоен на published — significant, потому что именно этот процесс, а не workspace, может быть тем, что реально торгует.)

Дополнительно, менее срочно: `Telegram webhook url: null` — сейчас ни один процесс не обрабатывает входящие Telegram-команды (ни webhook, ни polling у workspace). Это временно отключено намеренно, или это gap, из-за которого `/status` и другие админ-команды сейчас не работают?

До ответа на пункты 1-3 — deployment не обсуждаю, независимо от качества diff. Это может быть более серьёзный структурный вопрос, чем сам #182.