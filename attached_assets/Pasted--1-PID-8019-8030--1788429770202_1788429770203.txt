Принято — фиксирую как выполненное действие.

Дев, подтверди по факту:
1. `PID 8019`/`8030` действительно завершены (не просто workflow marked stopped, как было с `failed`-статусом ранее) — реальная проверка процесса.
2. С этого момента единственный источник Bybit-действий — published.

Тудор, по фразе "бот снова не открывает новых позиций" — уточни: это то же самое давнее наблюдение (тот периодический симптом, который расследовали ещё раньше, до этого forensic-разбирательства), или просто то, что прямо сейчас нет открытых позиций — и это может быть нормально (нет сигналов, подходящих под критерии, или сработал exposure cap), а не патология. Если это тот же повторяющийся симптом — стоит иметь в виду, что часть более ранних "необъяснимых" случаев теперь может объясняться найденным сбросом БД при publish, а не отдельным багом; но если сейчас видно что-то новое (например, сигналы генерируются, но не превращаются в позиции) — опиши, что именно наблюдаешь.

Важнее самого вопроса "всё ли работает" — то, что скрыто в твоём же отчёте: **workspace runtime сам автоматически перезапустился** сразу после того, как его явно остановили по моей срочной просьбе ("Проверю текущий статус после автоматического перезапуска" — это твои слова). То есть остановка была отменена системой без чьего-либо "да", в течение того же расследования. Это не разовый инцидент — это тот же паттерн авто-рестарта, который уже фиксировали раньше, и теперь он подтверждён как быстро срабатывающий и рушащий любую ручную остановку.

Это меняет статус пункта "убрать Telegram Webhook Bot из composite `Project` workflow" — раньше он был опциональным, теперь это обязательное условие, иначе dual-runtime будет возвращаться после каждой попытки остановки.

**Нужно:**

1. **Дев** — прежде чем что-либо чинить, определи точно, что именно вызвало этот авто-рестарт (composite `Project` workflow с restart-on-crash, или отдельный health-check/supervisor). Без этого повторная остановка снова окажется временной.
2. **Остановить workspace ещё раз** — прямо сейчас он опять активен и потенциально торгует параллельно с published.
3. **Тудор** — нужно отдельное явное "да" на две вещи: (а) повторную остановку workspace, и (б) саму конфигурационную правку, убирающую авто-рестарт (например, исключение `Telegram Webhook Bot` из composite workflow) — это уже не опциональный пункт, а обязательный для того, чтобы п.1 из списка тикетов вообще закрылся.

Отдельно, дев — уточни: цифры "0 новых bybit_demo_positions за 6 часов" и "последняя строка 2 сентября 20:45 UTC" взяты из **какой именно** базы — workspace `alerts.db` или published? С учётом подтверждённого расхождения баз это меняет интерпретацию: возможно, published за это время что-то писал независимо, даже пока workspace был down.

По остальному — анализ разумный и не тревожный сам по себе: `oversold_24h`/`new_listing` вне whitelist, поэтому не должны были доходить до Bybit — это ожидаемое поведение, не баг. Gate.io-деградация (`semaphore_wait_timeout`, prefetch budget) — уже известный, отдельно тикетированный follow-up по `range_breakout_long`, новых действий сейчас не требует.