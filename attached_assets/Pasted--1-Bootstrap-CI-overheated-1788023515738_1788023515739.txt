Три пункта по текущим результатам — до продолжения работы.

## 1. Bootstrap CI на ячейку `overheated_confirmed`/2/3 (#153 follow-up)

n=20, avg R = −0.419, WR на 15 п.п. ниже безубыточного — ровно на границе достаточности выборки. Прежде чем считать это подтверждённым основанием избегать уровня 2/3, посчитайте paired bootstrap 95% CI на avg R (или на дельту WR−breakeven) именно для этой ячейки — тот же метод, что уже использовали в #150/#152 (ресемплинг по уникальному signal ID, 20 000 итераций, percentile 95%). Если CI не пересекает ноль — это уже достаточно твёрдое основание. Заодно посчитайте CI и для `3/3` (n=6) — просто чтобы явно показать, что вывод там не делается, а не потому что забыли.

## 2. Недостающие цифры и вопрос по комиссии (#152 follow-up)

В прошлом отчёте по частичному TP/trailing было только «все 95% CI выше нуля», без самой таблицы. Пришлите:

- ΣR / WR / avg R для baseline и для каждого из 7 шагов грида (`overheated_24h`), как это было оформлено в #150.
- Разбивку исходов: сколько branch-step комбинаций закрылось ровно на полу (совпадение с baseline) против реального trailing-выхода выше пола — по каждому шагу отдельно, не только итоговую сумму 1055/100.
- Учтена ли дополнительная комиссия/slippage за второй leg закрытия позиции (раньше один close на TP, теперь два: 50%+50%)? Если не учтена — насколько это может съесть от отчётного avg R по грубой оценке.

## 3. Нумерация тикетов — коллизия

Мой #153 («разбить cohort WR/avg R по уровню подтверждения») уже выполнен и, судя по названию каталога `outcome_confirmation_level_analysis`, корректно занял номер #153. Но в предыдущем сообщении вы же предложили **#153 и #154** для двух других вещей — forward-проверки partial-TP и отдельного анализа EMA Cross со ступенчатым TP. Это разные задачи под одинаковыми номерами. Сверьтесь с реальным трекером, дайте правильные номера, и уточните: «EMA Cross со ступенчатым TP» — это то же самое, что нужно сделать для лестницы `ema_cross_confirmed` в рамках идеи B (#152), или отдельный вопрос?
