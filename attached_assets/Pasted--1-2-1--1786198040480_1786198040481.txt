Задача из двух этапов. Сначала этап 1, жди моего выбора, 
потом этап 2.

ЭТАП 1 — АУДИТ (ничего не менять в коде!)
Проанализируй app.py и ответь списком:

1. Какие источники данных бот УЖЕ использует 
   (эндпоинты Binance/Bybit, индикаторы).
2. Какие БЕСПЛАТНЫЕ данные доступны, но НЕ используются. 
   Проверь конкретно:
   • Binance Futures public API: openInterest, 
     fundingRate, topLongShortAccountRatio, 
     topLongShortPositionRatio, takerlongshortRatio, 
     forceOrders (ликвидации)
   • Bybit V5 public API: open-interest, funding/history, 
     account-ratio, liquidation
   • CoinGlass API (free tier): агрегированные ликвидации, 
     OI по всем биржам, funding rate heatmap
   • Whale Alert (free): крупные переводы на биржи
   • Etherscan API (free): мониторинг заданных кошельков
3. Для каждого пункта напиши:
   • что это даёт для качества сигналов (1-2 строки)
   • сложность внедрения: лёгкая / средняя / сложная
   • нужен ли API-ключ и есть ли лимиты free-тарифа
4. Предложи, КАК каждый источник встроить в текущий 
   скоринг сигналов (бонус/штраф к баллам или фильтр-вето).
   Пример логики, которую я хочу:
   • SHORT-сигнал + funding rate сильно положительный 
     (>0.05%) = +10 баллов (толпа в лонгах)
   • SHORT-сигнал + только что каскад ликвидаций шортов 
     = -15 баллов или вето (риск шорт-сквиза уже реализован)
   • Резкий рост OI + рост цены = подтверждение тренда
5. Выдай итоговую таблицу-меню: номер, название фичи, 
   польза, сложность. Я выберу номера — только их 
   и будешь внедрять.

ЭТАП 2 — ВНЕДРЕНИЕ (только после моего выбора)
Требования к реализации выбранных фич:
• Все новые данные кэшировать (funding/OI обновлять раз 
  в 1-5 минут, не на каждый тик) — не превышать 
  rate-лимиты бесплатных тарифов.
• Каждый новый фактор добавить в текст сигнала отдельной 
  строкой, например: "💰 Funding: +0.08% (лонги платят) → 
  +10 баллов".
• Все новые модули с обработкой ошибок: если API недоступен 
  — сигнал работает как раньше, без этого фактора, 
  в лог пишется предупреждение.
• Для каждого нового фактора вести статистику в демо-режиме: 
  winrate сигналов С фактором и БЕЗ него, чтобы потом 
  отключить бесполезные.
• Никаких платных API. Если нужен бесплатный ключ 
  (CoinGlass, Etherscan, Whale Alert) — скажи мне, 
  я зарегистрируюсь и добавлю в Secrets.
• Все тексты на русском.
