Прошёл оба diff'а построчно — фикс закрывает оба требования корректно.

**Главный фикс.** `_safe_post_entry_tp_setup()` оборачивает весь вызов `_ensure_tp_orders_for_ledger(...)` (шире, чем я формально просил — не только внутренний lookup, а всю функцию целиком, что надёжнее), `except Exception` с деградацией в `recovery_required` без влияния на статус entry. Оба call site в `_submit_entry_order()` (и "recovered"-ветка, и основной путь) теперь используют именно эту обёртку — проверил, оба хука заменены. Новый тест `test_multi_tp_post_entry_lookup_error_does_not_reject_entry` целит ровно в уязвимый путь: `entry_status="New"` заставляет код дойти до незащищённого раньше lookup, инъекция ошибки именно на этот `order_id`, и assert `result["status"] == "submitted"` — это прямое доказательство, что успешный entry больше не может быть искажён до "rejected" из-за сбоя в TP-setup.

**Deterministic reject → `recovery_required_manual`.** Обе точки, где мог возникнуть `"rejected"` (проверка persisted-статуса existing legs и проверка результата свежей попытки размещения), теперь корректно ведут в отдельный `BYBIT_DEMO_TP_MANUAL_RECOVERY_STATE`, а `"ambiguous"/"duplicate"` остаются в обычном retryable `recovery_required`. SELECT в `ensure_pending_tp_orders()` не трогался и не включает новое состояние — тест это прямо подтверждает: `pending["processed"]` упал с 1 (старое поведение) до 0 (новое) на той же самой сценарии deterministic reject. Это ровно то разделение retryable/non-retryable, которое я просил.

Арифметика (121→122 в `test_bybit_demo.py`, 127 суммарно, targeted 27→28) пересчитал — сходится.

## Один пункт для отдельного уточнения, не блокирует review этого фикса

В автоматическом коммите `c396952` оказались два файла не по теме — `outcome_trailing_shadow/coverage.json` и `forward_analysis.md`. Хорошо, что явно указал это, а не спрятал. Уточни: это уже существовавшие незакоммиченные изменения из отдельного, не связанного потока работы (shadow-trading outcome tracking), которые случайно затянулись в этот auto-commit? Если да — стоит развести их в отдельный коммит для чистоты истории, хотя для review этого TP-фикса это не имеет значения, раз они не пересекаются с проверенными файлами.

**Этап 5 (с обоими фиксами) принят.**

Из открытого: follow-up #199 (операторский путь восстановления `recovery_required_manual`) — присылай план отдельно, когда будет актуально. Этап 6 (breakeven SL после TP1) остаётся впереди, ещё не начат.