Построчный review — чисто, без замечаний, которые бы блокировали продолжение.

Хорошо, что заметно применение более раннего урока: `_tp_event()` использует `INSERT OR IGNORE` и **проверяет `cursor.rowcount`** перед тем как считать событие вставленным (`return int(cursor.lastrowid) if cursor.rowcount else None`) — это ровно та проверка, отсутствие которой мы подозревали как причину silent-branch в куда более раннем расследовании. Хорошо, что здесь она сразу на месте.

Проверил:
- CREATE TABLE и ALTER-цикл добавляют идентичный набор из 15 полей — миграция на существующей БД и создание с нуля не разойдутся.
- `bybit_demo_tp_legs` — `UNIQUE(ledger_id, leg_index)`, корректно не идемпотентна (обычный INSERT, не OR IGNORE) — это осознанно правильно: попытка вставить дубликат leg должна кидать ошибку, а не молча проглатываться, в отличие от событий.
- `bybit_demo_tp_events` — `UNIQUE(ledger_id, idempotency_key)`, идемпотентна через OR IGNORE — соответствует плану.
- `_update_row()` использует allowlist для новых полей, unknown-поля по-прежнему кидают ошибку — не открывает произвольную запись в БД.
- Тест `test_tp_leg_and_event_model_is_idempotent` доказывает именно то, что нужно: повторный вызов с тем же idempotency key не создаёт вторую строку и не перезаписывает данные первой (это `OR IGNORE`, не `OR REPLACE` — правильный выбор).
- `test_initialize_schema_adds_backward_compatible_tp_model` — двойной вызов `initialize_schema()` безопасен, legacy-таблица без новых колонок корректно мигрирует, defaults подтверждены.

Один вопрос — не блокирует этот этап, но держи в уме на этапе 2: `_insert_tp_leg()` при повторном вызове с тем же `(ledger_id, leg_index)` кинет необработанный `IntegrityError` (это, в отличие от событий, обычный INSERT). Когда calculator начнёт реально вызывать этот helper — уточни, предполагается ли, что вызывающий код сам гарантирует single-call без ретраев, или нужна отдельная защита на уровне calculator/poll.

Этап 1 принят. Можно переходить к этапу 2 (ATR/TP/split calculator) — присылай на review так же, отдельным diff, когда будет готово.