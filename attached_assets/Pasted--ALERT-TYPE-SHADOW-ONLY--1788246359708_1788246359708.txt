Вот готовый текст — можно копировать как есть:

---

Уточнения по логике `ALERT_TYPE_SHADOW_ONLY` и по разбивке 459/35 принимаются, спасибо. Но всплыл более срочный вопрос, который нужно закрыть до финального вывода по `overheated_24h`.

**Как `overheated_confirmed`/`ema_cross_confirmed` вообще получают `is_shadow=False` после фикса `19fb443`?**

Смотрю на последовательность из твоего же ответа:

1. Обе стратегии отсутствуют в `ALERT_TYPE_SHADOW_ONLY` → при `SHADOW_ONLY_MODE=True` они попадают в ветку `elif SHADOW_ONLY_MODE: return (False, None)` в `send_alert_with_log()` — то есть должны быть shadow всегда, независимо от Bybit-гейта.
2. До `19fb443` (30 авг, 09:20 UTC) Bybit-гейт для confirmed-типов не проверял `is_shadow` — поэтому shadow-сигналы всё же прорывались в Bybit. Это была утечка/баг, а не легитимный live-путь.
3. После `19fb443` добавлено `not is_shadow` — любой shadow-источник теперь блокируется до Bybit.

Если `send_alert_with_log()` для confirmed-типов не менялся и они по-прежнему всегда попадают в shadow-ветку по глобальному `SHADOW_ONLY_MODE`, то **после 30 августа обе confirmed-стратегии тоже должны быть полностью заблокированы** — тем же механизмом, что и `overheated_24h`, просто раньше их прикрывала утечка.

Если это так — текущие "2 открытые позиции" не доказывают, что whitelist работает для 2 из 3 стратегий, а являются остатком утечки до 30 августа. И прямо сейчас ни одна из 3 whitelisted стратегий может не иметь рабочего live-пути на Bybit вообще.

**Прошу проверить и ответить:**

1. Есть ли где-то ещё в коде путь, который выставляет `is_shadow=False` для `overheated_confirmed`/`ema_cross_confirmed` (отдельная ветка, per-strategy override, флаг `overheated_early_promoted` и т.п.)? Если да — показать точный фрагмент.
2. Если такого пути нет — когда была создана **последняя** реальная (`is_shadow=0`) позиция для каждой из этих двух стратегий? Это подтвердит или опровергнет: "утечка закрылась 30 августа, и с тех пор live-путь мёртв для всех трёх стратегий, а не только для overheated_24h".
3. Если гипотеза подтвердится — это уже не локальная проблема одной стратегии, а regression, внесённый самим (правильным по логике) фиксом `19fb443` — нужен отдельный приоритетный тикет.

Пока это не прояснено, выводы по `overheated_24h` (459/35, shadow gate) принимаю как корректные сами по себе, но не как полную картину — реальный ответ на "почему мало позиций" может оказаться шире, чем одна стратегия.