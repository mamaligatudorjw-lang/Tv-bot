Проверил арифметику по всем четырём отчётам — везде реконсиляция сходится точно, и (что важнее) во всех подгруппах суммы корректно сводятся к общему итогу:

- **#108**: 109=20+57+32+0 ✓; conflict(50)+no-conflict(59)=109, TP/SL/unresolved по подгруппам тоже сходятся к общим 20/57/32 ✓; pre_fix(107)+post_fix(2)=109 ✓; post_fix avg R=0.500 точно соответствует (1×+2R + 1×−1R)/2 — формула подтверждена на чистом примере.
- **#109**: 620=214+206+148+52 ✓; ema_cross(330)+confirmed(290)=620, и все 4 категории по стадиям складываются в общие цифры ✓.
- **#110**: 161=65+70+26+0 ✓; pre(158)+post(3)=161 ✓; post_fix avg R=−1.000 точно соответствует 3×(−1R)/3 — снова формула подтверждена.
- **#111**: 293=18+68+201+6 ✓.

Всё арифметически корректно. Но есть содержательные вопросы, прежде чем закрывать любую из четырёх:

**#108** — MAE/time-to-resolution всё ещё не сделаны, а это была ключевая причина запроса (её позиция была в просадке "больше суток"). Уточните: текущее 24h-окно вообще способно корректно классифицировать такой кейс, или для `pump_24h_fade` нужно расширенное окно, раз реальные позиции могут разрешаться позже 24ч?

**#109** — реконструкция confirm#1/2/3 всё ещё не готова. Есть ли путь восстановить номер из `continuation_pending`/логов для уже прошедших сигналов, или для этого нужна отдельная доработка, фиксирующая номер подтверждения при записи в `demo_positions` (тогда это будет доступно только для будущих сигналов)?

**#110** — расхождение доставки теперь выглядит иначе и серьёзнее: раньше был разрыв в 1 (132/133), сейчас — 21 (144 delivered / 165 в БД). Это может быть просто ростом выборки со временем, а не тем же расхождением, но нужна явная сверка по `symbol + timestamp + pid`, как и договаривались, а не просто новые агрегаты.

**#111** — два вопроса: (1) unresolved-доля здесь 68.6% (201/293) — заметно выше, чем у #108 (29%), #109 (24%) и #110 (16%). Это нормально для `confluence` (например, более широкие TP/SL) или указывает на проблему с окном/данными? (2) Это только shadow-часть (293 из 295 shadow-строк). Реальные (non-shadow) 385 `confluence`-позиций не проанализированы вообще — а именно `confluence` был identifицирован ранее как стратегия за MEMEUSDT-сигналом, из-за которого всё расследование и началось. Полный ответ по #111 без non-shadow-среза неполон для исходной цели. Также: почему для read-only отчёта вообще потребовалась вставка в `demo_positions` и упёрлась в уникальный индекс — отчёт не должен писать в БД, только читать `SELECT ... WHERE alert_type='confluence'` отдельно для `is_shadow=0` и `is_shadow=1` и сводить в Python/pandas, без UNION на уровне SQL с вставками.

Отправляю?