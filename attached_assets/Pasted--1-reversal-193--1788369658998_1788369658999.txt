Сообщение для дева:

---

Три пункта, нужны разом:

**1. Полный файл reversal-модуля для #193 (не фрагментами)**

Присланных ранее выдержек недостаточно для финального решения — нужен весь код вокруг reversal-логики в `bybit_demo.py` целиком (или отдельный модуль, если он вынесен), чтобы закрыть 4 открытых пункта:
- Генерация `order_link_id` для open-ноги — покажи конкретное значение/функцию, отличается ли оно от close-ключа (`_reversal_close_link_id`). Если используется одна и та же переменная — это баг: idempotency-check на open найдёт close-ордер по совпадающему ключу и не отправит open вообще.
- Полный `_claim_reversal()` — нужен общий `except Exception: rollback(); raise` (или `finally`) вокруг `BEGIN IMMEDIATE`, не только `except sqlite3.IntegrityError`, иначе непойманное исключение оставит незакоммиченную транзакцию и заблокирует запись во всю БД.
- Что происходит в ветке "не retryable, не transport" при ошибке close — покажи код после `if exc.retryable or exc.transport:`.
- Есть ли активный watchdog (scheduled job), который проверяет зависшие `CLOSING`/`OPEN_PENDING` с истёкшим `close_deadline_ts`, или восстановление только реактивное (по новому сигналу)? Если только реактивное — нужен отдельный периодический скан, иначе запись может зависнуть неопределённо, если по символу долго не будет сигналов.

Также покажи реализацию мока `get_order_realtime()` в тестовом `ReversalTradingClient` — он должен различать close/open по фактическому `order_link_id`, а не возвращать пусто безусловно, иначе тест `test_reversal_uses_two_orders_close_then_open` не может считаться доказательством отсутствия коллизии.

**2. Пересобрать #188 (24h rollback-отчёт по #186) на данных published, не workspace**

Весь прежний анализ по #186 использовал `alerts.db` от workspace — процесса, который не обслуживал реальный Telegram-трафик. Нужен свежий отчёт по тем же критериям (`bybit_demo_order_rejected` vs `Filled` ratio, дубли позиций, необработанные exceptions), но по published ledger — единственному сейчас активному источнику правды.

**3. Ответ на вопрос про TP/SL при ручном добавлении USDT**

Остаётся без ответа с самого начала: закрывает ли бот позицию по своему ledger-размеру или по реальному размеру на бирже в момент срабатывания TP/SL. Нужен точный код `poll_positions()`/аналогичной close-логики.

---

Жду ответ по всем трём. Deployment #193 остаётся заблокирован до закрытия пункта 1.